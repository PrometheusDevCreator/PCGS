# Templates Directory

This directory stores the templates used for generating exports (PPTX, DOCX).

## Structure

- `default/`: Generic templates.
- `rabdan/`: Rabdan Academy specific templates.

## Usage

The `pcgs_exports` module will look here to find the appropriate `.pptx` or `.docx` file to use as a base for generation.







